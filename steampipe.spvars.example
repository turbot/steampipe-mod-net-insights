# Domain names used in DNS and SSL checks/reports.
# Should not include http:// or https:// and must include a port number at the
# end
domain_names = [ "github.com:443", "microsoft.com:443" ]

# Website URLs used in HTTP checks/reports, must include http:// or https://
website_urls = [ "https://github.com", "https://microsoft.com"]
